import { integer, primaryKey, sqliteTable, text } from "drizzle-orm/sqlite-core";

export const trips = sqliteTable("trips", { id: text("id").primaryKey(), destination: text("destination"), startDate: text("start_date"), endDate: text("end_date"), travelers: integer("travelers").notNull().default(2), interestsJson: text("interests_json").notNull().default("[]"), budgetLimit: integer("budget_limit"), focusedDestinationId: text("focused_destination_id"), version: integer("version").notNull().default(1), createdAt: text("created_at").notNull(), updatedAt: text("updated_at").notNull() });
export const candidates = sqliteTable("trip_candidates", { tripId: text("trip_id").notNull().references(() => trips.id, { onDelete: "cascade" }), destinationId: text("destination_id").notNull(), source: text("source", { enum: ["user", "agent"] }).notNull(), position: integer("position").notNull() }, (table) => [primaryKey({ columns: [table.tripId, table.destinationId] })]);
export const itinerary = sqliteTable("itinerary_items", { id: text("id").primaryKey(), tripId: text("trip_id").notNull().references(() => trips.id, { onDelete: "cascade" }), day: integer("day").notNull(), position: integer("position").notNull(), time: text("time").notNull(), title: text("title").notNull(), location: text("location").notNull(), note: text("note").notNull(), estimatedCost: integer("estimated_cost").notNull(), source: text("source", { enum: ["user", "agent"] }).notNull() });
export const budgets = sqliteTable("trip_budgets", { tripId: text("trip_id").primaryKey().references(() => trips.id, { onDelete: "cascade" }), transport: integer("transport").notNull().default(0), stay: integer("stay").notNull().default(0), food: integer("food").notNull().default(0), activities: integer("activities").notNull().default(0) });
export const chatMessages = sqliteTable("chat_messages", { id:text("id").primaryKey(),tripId:text("trip_id").notNull().references(()=>trips.id,{onDelete:"cascade"}),role:text("role",{enum:["user","assistant"]}).notNull(),content:text("content").notNull(),status:text("status",{enum:["pending","completed","failed"]}).notNull(),sequence:integer("sequence").notNull(),createdAt:text("created_at").notNull(),updatedAt:text("updated_at").notNull() });
export const toolActivities = sqliteTable("tool_activities", { id:text("id").primaryKey(),tripId:text("trip_id").notNull().references(()=>trips.id,{onDelete:"cascade"}),messageId:text("message_id").notNull().references(()=>chatMessages.id,{onDelete:"cascade"}),toolName:text("tool_name").notNull(),label:text("label").notNull(),status:text("status",{enum:["running","completed","failed"]}).notNull(),sequence:integer("sequence").notNull(),createdAt:text("created_at").notNull(),completedAt:text("completed_at") });
